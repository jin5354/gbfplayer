import React from 'react';
import GameDataStore from '../stores/GameDataStore';
import AppDispatcher from '../dispatcher/AppDispatcher';
import '../../scss/antd.less';
import {Table} from 'antd';

class NetLog extends React.Component {
    componentDidMount() {
        //GameDataStore.addEventListener('HTTPDataUpdate', this.forceUpdate.bind(this));
    }
    getData() {
        return GameDataStore.getReq
    }
    render() {
        return (
            <div id="NetLog">
                <Table columns={columns} dataSource={getData()} size="small" />
            </div>
        );
    }
}

export default NetLog;
